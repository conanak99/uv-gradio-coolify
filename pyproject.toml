[project]
name = "uv-fal"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "gradio>=6.20.0",
    "fal-client>=0.5",
    "python-dotenv>=1.0",
    "Pillow>=10.0",
]
